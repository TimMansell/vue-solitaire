import { findUserByLID, userStats, globalStats } from './resolvers/query';

// eslint-disable-next-line import/prefer-default-export
export const queries = {
  findUserByLID,
  userStats,
  globalStats,
};
